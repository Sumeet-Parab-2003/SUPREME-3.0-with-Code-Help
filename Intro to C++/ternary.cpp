#include<iostream>
using namespace std;
 int main(){
    // int age;
    // cout << "Enter ur age : ";
    // cin >> age ;

    // (age > 18) ? cout << "U can vote" : cout << "U cannot vote";

    int x = 10;
    int y = 20;

    int result = (x > y) ? x : y;

    cout << "Result = " << result ;

    return 0;
 }